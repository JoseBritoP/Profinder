const Certificates =()=>{
   // const session = useSessionState((state) => state.session);

    return (
        <div>
            <h1>
                aca van los certificates
            </h1>
        </div>
    )
}


export default Certificates